int factorial(int n) {
    if (n <= 1) {
        return 1;
    }
    return n * factorial(n - 1);
}

void printNumber(int x) {
    print(x);
    return;
}

int main() {
    int n := 5;
    int result := factorial(n);
    printNumber(result);
    return 0;
}